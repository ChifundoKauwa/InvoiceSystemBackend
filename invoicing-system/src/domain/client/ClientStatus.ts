/**
 * DOMAIN LAYER: Client Status Enum
 * 
 * Represents the lifecycle states of a client.
 */
export enum ClientStatus {
    Active = "active",
    Inactive = "inactive",
    Archived = "archived"
}
